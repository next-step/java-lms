package nextstep.courses.domian;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class SessionTest {

    @Test
    @DisplayName("만일 무료 강의면서 처음 신청하는 사람이라면 정상 신청된다.")
    void sessionStudent_max() {
//        Session session = new Session(1L, "test", StudentsTest.ONE_STUDENTS, SessionDurationTest.SD, true, SessionStatusType.RECRUITMENT, CoverImageTest.COVER_IMAGE, 0L);
//
//        Assertions.assertThat(session.enrolment(NsUserTest.JAVAJIGI, 0L))
//                .isEqualTo(new Payment(1L, NsUserTest.JAVAJIGI.getId(), 0L));
    }

}
