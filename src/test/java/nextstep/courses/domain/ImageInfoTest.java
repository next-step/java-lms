package nextstep.courses.domain;

import static org.junit.jupiter.api.Assertions.*;

public class ImageInfoTest {

    public static final ImageInfo imageInfo = new ImageInfo(new Size(500), new Frame(300, 200));
}