package nextstep.courses.domain;

import nextstep.users.domain.NsUserTest;


class JoinUserTest {

    public static final JoinUser JAVAJIGI = new JoinUser(NsUserTest.JAVAJIGI, null);
    public static final JoinUser SANJIGI = new JoinUser(NsUserTest.SANJIGI, null);
}
