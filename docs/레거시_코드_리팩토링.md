# 1단계 - 레거시 코드 리팩토링

## 기능 요구사항
- 질문 데이터를 완전히 삭제하는 것이 아니라 데이터의 상태를 삭제 상태(deleted - boolean type)로 변경한다.
- 로그인 사용자와 질문한 사람이 같은 경우 삭제 가능하다.
- 답변이 없는 경우 삭제가 가능하다.
- 질문자와 답변 글의 모든 답변자가 같은 경우 삭제가 가능하다.
- 질문을 삭제할 때 답변 또한 삭제해야 하며, 답변의 삭제 또한 삭제 상태(deleted)를 변경한다.
- 질문자와 답변자가 다른 경우 답변을 삭제할 수 없다.
- 질문과 답변 삭제 이력에 대한 정보를 DeleteHistory를 활용해 남긴다.

## 리팩토링 요구사항
- nextstep.qna.service.QnaService의 deleteQuestion()는 앞의 질문 삭제 기능을 구현한 코드이다. 
  이 메소드는 단위 테스트하기 어려운 코드와 단위 테스트 가능한 코드가 섞여 있다.
- QnaService의 deleteQuestion() 메서드에 단위 테스트 가능한 코드(핵심 비지니스 로직)를 도메인 모델 객체에 구현한다.
- QnaService의 비지니스 로직을 도메인 모델로 이동하는 리팩터링을 진행할 때 TDD로 구현한다.
- QnaService의 deleteQuestion() 메서드에 대한 단위 테스트는 src/test/java 폴더 nextstep.qna.service.QnaServiceTest이다. 
  도메인 모델로 로직을 이동한 후에도 QnaServiceTest의 모든 테스트는 통과해야 한다.

## 힌트
- 객체의 상태 데이터를 꺼내지(get)말고 메시지를 보낸다.
- 규칙 8: 일급 콜렉션을 쓴다.
  - Question의 List를 일급 콜렉션으로 구현해 본다.
- 규칙 7: 3개 이상의 인스턴스 변수를 가진 클래스를 쓰지 않는다.
  - 인스턴스 변수의 수를 줄이기 위해 도전한다.
- 도메인 모델에 setter 메서드 추가하지 않는다.

## 기능 구현 목록(리팩토링 목록)
- [x] 질문을 삭제한다
  - [x] 질문을 삭제할 때 boolean type으로 삭제 상태를 변경한다
  - [x] 로그인 사용자와 질문한 사람이 같은 경우에 삭제가 가능하다
    - [x] 다른 경우에는 예외가 발생한다
    - [x] 답변이 없는 경우에 삭제가 가능하다
    - [x] 질문자와 답변 글의 모든 답변자가 같은 경우 삭제가 가능하다
      - [x] 다른 경우에는 예외가 발생한다
      - [x] 질문을 삭제할 때는 답변도 삭제해야 하며 답변도 boolean type으로 삭제 상태를 변경한다
      - [x] 답변을 삭제할 때도 로그인 사용자와 같은 경우에 삭제가 가능하며 다른 경우에는 예외가 발생한다
- [x] Question의 List를 일급 콜렉션으로 구현한다
  - [x] Question 내의 Answer 리스트를 일급 콜렉션으로 구현한다
    - [x] Answers 내에서 답변 리스트 생성, 추가, 삭제 등의 기능을 추가한다
- [x] 질문과 답변 삭제 이력에 대한 정보를 DeleteHistory를 활용해 남긴다
- [x] Question 내의 title과 contents를 QuestionContent로 분리한다
- [ ] 공통 엔티티 속성을 BaseEntity로 분리한다
  - [ ] 생성/수정 시간 중복을 제거한다
  - [ ] 삭제 가능한 엔티티는 DeletableBaseEntity로 분리한다
