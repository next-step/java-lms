# 질문 삭제하기 
- 로그인 사용자 = 질문한 사용자 -> 삭제 가능
- 답변 없음 -> 삭제 가능
- 질문 삭제 -> 답변 삭제 : 모두 soft delete
  - 질문자와 답변 글의 모든 답변자가 같으면 삭제 가능
- 질문자 != 답변자 -> 답변 삭제 불가능
  - 질문과 답변 삭제 이력에 대한 정보를 DeleteHistory를 활용해 남기기 

## deleQuestion() 메서드
- 단위 테스트 가능한 코드를 도메인 모델 객체에 구현하기 
- 도메인 모델로 이동하는 리팩터링을 진행할 때 TDD로 구현하기 